# TODO

editor:
- column icons missing
- tooltip show ITM number -- remove that tootip
- editable icon chnage to something else (icons are inconsisten, one look like icons others like drafts in qt)
- wire up `Exclude from Merge` and `Skip if Missing` sub-record rules to auto_merge and conflict detection (currently only `Ignore Conflict` works) -- skip if missing only applicable to CELL so remove options and hardoce it. Exclude from Merge and Ignore Conflict is basically the same, should skip comparison/grey out subrecord and dont include it in merged patch.
- lua handlers visual inconsistecy with esp coloring, panels entries not colored and columns different sizes