# TODO

editor:
- column icons missing
- tooltip show ITM number
- editable icon chnage to something else
- wire up `Exclude from Merge` and `Skip if Missing` sub-record rules to auto_merge and conflict detection (currently only `Ignore Conflict` works)