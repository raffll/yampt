# TODO

editor: create new plugin just like merged patch and switch active plugin

translator: F10 (set status translated) applies only to the first selected row - make it apply to the whole selection, allow that for all quickkeys and right menu buttons, maybe merge them

translator: Statuses tab counts should reflect the active Filters selection (e.g. filtered to CELL shows counts within that subset), not always the whole dictionary

convert: script recompilation issue - converting Tamriel Data crashes to desktop on "new game"; recompiling the plugin in CS fixes it. Investigate SCDT/SCTX patching during --convert. Test dict: Tamriel_Data+Tamriel_Data_PL_BASE_XX-XX.rar

editor: sort menus, in nav and view tree