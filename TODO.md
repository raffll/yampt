# TODO

show optional subrecords in light grey, make them editable

sctx - script script 

left of apply button is space when explanaition why edit is not valid

is it possible to remove individual records from a plugin with this? 

translator: ai tyransaltor editable prompt

info chain can display info text and also do conflict comparison, if textr differ with name inam

route build_dict_entries (plugin_operations_controller.cpp) through canonicalize_path instead of its own to_lower(normalize_path) dedup scheme

strip trailing slash from resource_paths resolve_* and switch the ~7 concatenation sites to a path-join helper
