# TODO

- Replace [BASE] / [USER] text tags with icons — the sidebar currently shows plain text labels like [BASE] to distinguish dictionary kinds; replace these with small colored icons for quicker visual scanning

- Change yEditor icon for overridden records — the current override indicator is too similar to the normal record icon; use a visually distinct shape or color so overridden records stand out immediately in the nav tree

- Show enchantment name in annotations for FNAM entries — when an FNAM entry belongs to an enchanted item, display the enchantment ID in the annotations panel so the translator knows the item is enchanted

- Fix EET encoding issues
- Add Convert Plugin with Hyperlinks menu option
- Streamline shortcuts and right-click menu
- All operations that set In Progress/Replaced should advance row

- Dehardcode languages to external file — move the hardcoded language list (PL, DE, FR, RU, IT, HU) and their associated NLLB codes, codepages, and dictionary filenames into a JSON or INI config file so new languages can be added without recompiling