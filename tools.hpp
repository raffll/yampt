#ifndef TOOLS_HPP
#define TOOLS_HPP

#include <string>
#include <vector>

using namespace std;

class tools
{
public:
	vector<string> dict_name = {"cell", "gmst", "fnam", "desc", "book", "fact", "indx", "dial", "info", "scpt"};
};

#endif
