# Cleanest Approach

Always use the cleanest approach, not the easiest one. Prefer proper architecture over shortcuts — forward declarations in dedicated internal headers over extern hacks, proper file splits over stuffing related code into one file with comments, and real abstractions over type-punning tricks.

